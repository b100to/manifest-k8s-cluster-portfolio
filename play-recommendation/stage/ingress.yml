apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: play-recommendation-stage-internal
  namespace: default
  annotations:
    alb.ingress.kubernetes.io/load-balancer-name: "play-recommendation-stage"
    alb.ingress.kubernetes.io/scheme: "internal"
    alb.ingress.kubernetes.io/target-type: "ip"
    alb.ingress.kubernetes.io/listen-ports: '[{"HTTP": 80}]'
spec:
  ingressClassName: alb
  rules:
    - http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: play-recommendation-stage
                port:
                  name: http
