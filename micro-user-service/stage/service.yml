apiVersion: v1
kind: Service
metadata:
  name: micro-user-service-stage
  labels:
    app.kubernetes.io/name: micro-user-service-stage
spec:
  ports:
    - port: 8871
      name: http
      targetPort: 8871
      nodePort: 30732
  selector:
    app.kubernetes.io/name: micro-user-service-stage
  type: NodePort
