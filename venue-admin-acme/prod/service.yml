apiVersion: v1
kind: Service
metadata:
  name: venue-admin-acme
  labels:
    app.kubernetes.io/name: venue-admin-acme
spec:
  ports:
    - port: 8092
      name: http
      targetPort: 8092
      nodePort: 30143
  selector:
    app.kubernetes.io/name: venue-admin-acme
  type: NodePort
