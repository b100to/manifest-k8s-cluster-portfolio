apiVersion: apps/v1
kind: Deployment
metadata:
  name: venue-partner-user-stage
spec:
  replicas: 0
  revisionHistoryLimit: 4
  selector:
    matchLabels:
      app.kubernetes.io/name: venue-partner-user-stage
  strategy:
      type: RollingUpdate
  template:
    metadata:
      labels: 
        app: venue-partner-user-stage
        version: v1.0
        app.kubernetes.io/name: venue-partner-user-stage
    spec:
      containers:
        - name: venue-partner-user-stage
          image: 222222222222.dkr.ecr.ap-northeast-2.amazonaws.com/venue-partner-user-stage
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet:
              path: /user/
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 5
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: stage