apiVersion: apps/v1
kind: Deployment
metadata:
  name: sc-ins-stage
spec:
  replicas: 0
  revisionHistoryLimit: 4
  selector:
    matchLabels:
      app.kubernetes.io/name: sc-ins-stage
  strategy:
      type: RollingUpdate
  template:
    metadata:
      labels: 
        app: sc-ins-stage
        version: v1.0
        app.kubernetes.io/name: sc-ins-stage
    spec:
      containers:
        - name: sc-ins-stage
          image: 222222222222.dkr.ecr.ap-northeast-2.amazonaws.com/sc-ins-stage
          ports:
            - containerPort: 8083
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: stage