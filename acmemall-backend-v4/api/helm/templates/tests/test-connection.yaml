apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "acmemall-backend-v4.fullname" . }}-test-connection"
  labels:
    {{- include "acmemall-backend-v4.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "acmemall-backend-v4.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
