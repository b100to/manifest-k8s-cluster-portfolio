apiVersion: v1
kind: Service
metadata:
  name: common-user-service-stage
  labels:
    app.kubernetes.io/name: common-user-service-stage
spec:
  ports:
    - port: 8871
      name: http
      targetPort: 8871
      nodePort: 30734
  selector:
    app.kubernetes.io/name: common-user-service-stage
  type: NodePort
