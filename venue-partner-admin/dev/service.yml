apiVersion: v1
kind: Service
metadata:
    name: venue-partner-admin-dev
    labels:
      app.kubernetes.io/name: venue-partner-admin-dev
spec:
  ports:
  - port: 8080
    name: http  
    targetPort: 8080
    nodePort: 30140
  selector:
    app.kubernetes.io/name: venue-partner-admin-dev
  type: NodePort