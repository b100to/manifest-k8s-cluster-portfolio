apiVersion: v1
kind: Service
metadata:
    name: acmemall-backend-dev
    labels:
      app.kubernetes.io/name: acmemall-backend-dev
spec:
  ports:
  - port: 8000
    name: http  
    targetPort: 8000
    nodePort: 30061
  selector:
    app.kubernetes.io/name: acmemall-backend-dev
  type: NodePort