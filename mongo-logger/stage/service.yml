apiVersion: v1
kind: Service
metadata:
  name: mongo-logger-stage
  labels:
    app.kubernetes.io/name: mongo-logger-stage
spec:
  ports:
    - port: 8092
      name: http
      targetPort: 8092
      nodePort: 30232
  selector:
    app.kubernetes.io/name: mongo-logger-stage
  type: NodePort
