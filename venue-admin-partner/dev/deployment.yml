apiVersion: apps/v1
kind: Deployment
metadata:
  name: venue-admin-partner-dev
spec:
  replicas: 0
  revisionHistoryLimit: 4
  selector:
    matchLabels:
      app.kubernetes.io/name: venue-admin-partner-dev
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        # app: venue-admin-partner-dev
        # version: v1.0
        app.kubernetes.io/name: venue-admin-partner-dev
    spec:
      containers:
        - name: app
          image: 111111111111.dkr.ecr.ap-northeast-2.amazonaws.com/venue-admin-partner-dev
          resources: {}
          ports:
            - containerPort: 8055
          readinessProbe:
            httpGet:
              path: /partner/
              port: 8055
            initialDelaySeconds: 30
            periodSeconds: 5
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: dev
