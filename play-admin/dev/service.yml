apiVersion: v1
kind: Service
metadata:
  name: play-admin-dev
  labels:
    app.kubernetes.io/name: play-admin-dev
spec:
  ports:
    - port: 80
      name: http
      targetPort: 8081
      nodePort: 30333
  selector:
    app.kubernetes.io/name: play-admin-dev
  type: NodePort
