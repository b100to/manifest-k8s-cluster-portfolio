apiVersion: v1
kind: Service
metadata:
    name: homepage-dev
    labels:
      app.kubernetes.io/name: homepage-dev
spec:
  ports:
  - port: 8081
    name: http  
    targetPort: 8081
    nodePort: 30360
  selector:
    app.kubernetes.io/name: homepage-dev
  type: NodePort