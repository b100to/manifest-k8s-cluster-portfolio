apiVersion: v1
kind: Service
metadata:
    name: homepage
    labels:
      app.kubernetes.io/name: homepage
spec:
  ports:
  - port: 8081
    name: http  
    targetPort: 8081
    nodePort: 30461
  selector:
    app.kubernetes.io/name: homepage
  type: NodePort